# OpenCode Restore & Fix Log — 2026-04-11

**Scope:** Restore OpenCode functionality after skills disappearance, Qwen OAuth breakage, and Modal GLM no-output.

---

## 1. Skills Missing

**Symptom:**  
- `enterprise-deep-debug` and other skills not found in OpenCode CLI.

**Root Cause:**  
- `~/.config/opencode/opencode.json` had `"skills": ["/Users/jeremy/.config/opencode/skills"]` (array), but the schema expects an object with a `paths` array.

**Fix:**
```json
"skills": {
  "paths": ["/Users/jeremy/.config/opencode/skills"]
}
```

Also ran `upgraded-opencode-stack/install.sh` to merge latest skills, commands, scripts, and agents without overwriting personal configs.

**Verification:**  
- Skills are now listed and usable.

---

## 2. Qwen OAuth Plugin Lost

**Symptom:**  
- `opencode providers list` showed no Qwen provider.
- `opencode run --model qwen/coder-model` returned `401 invalid access token or token expired`.
- Previous plugin `opencode-qwen-proxy` was broken and removed.
- User could not find Qwen login via `opencode auth login`.

**Root Cause:**  
- `opencode-qwen-proxy` was deprecated/removed. Proper plugin is `opencode-qwen-auth` which uses device-flow OAuth.
- The plugin was missing from both `~/.config/opencode/opencode.json` and the `upgraded-opencode-stack` install script.

**Fixes Applied:**
1. Removed `opencode-qwen-proxy` from:
   - `~/.config/opencode/opencode.json`
   - `/Users/jeremy/dev/upgraded-opencode-stack/opencode.json`
   - `/Users/jeremy/dev/upgraded-opencode-stack/plugins/local-plugins/` (cleaned up)
2. Installed `opencode-qwen-auth`:
   - Added to `install.sh` loop for local-plugins.
   - Updated `upgraded-opencode-stack/README.md` plugin table to reflect 5 plugins now.
   - Merged config via `./install.sh` which added the plugin entry.
3. Global `opencode.json` plugin list now includes `"opencode-qwen-auth"` (installed globally).
4. Provider definition for Qwen remained correct:
   ```json
   "provider.qwen": {
     "npm": "@ai-sdk/openai-compatible",
     "options": { "baseURL": "https://portal.qwen.ai/v1" },
     "models": {
       "coder-model": { "name": "Qwen 3.6 Plus", "limit": { "context": 1048576, "output": 65536 } },
       "vision-model": { "name": "Qwen 3.6 Vision Plus", "limit": { "context": 131072, "output": 32768 }, "attachment": true }
     }
   }
   ```

**Action Required:**
- User must run: `opencode providers login --provider qwen --method "Qwen OAuth"` and follow the device code link (e.g., `https://chat.qwen.ai/authorize?user_code=...`) to grant access. After that, Qwen tokens are stored and rotation works.

---

## 3. Modal GLM-5.1 No Output

**Symptom:**  
- `opencode run "echo test" --model modal/glm-5.1-fp8` returned only `step_start`/`step_finish` with zero tokens and no assistant text.
- Direct `curl` to Modal API with leased key worked and returned `"content":"test"`.

**Root Causes Investigated:**
- **Proxy streaming behavior:** The original modal proxy on OCI VM (`/opt/modal-token-pool/modal_proxy.py`) returned SSE stream when `stream:true` or when path contained "stream". OpenCode expected a non-streaming response, causing an `IncompleteRead` or empty result.
- **Model name mismatch:** OpenCode model ID `modal/glm-5.1-fp8` vs Modal's actual model `zai-org/GLM-5.1-FP8`. The proxy did not translate the model name, leading to `404 Unknown model` for some request patterns.
- **Proxy version drift:** The OCI VM was running an older, unpatched version of the proxy, despite local changes. File copy via `scp`/`ssh` had not overwritten the system file due to permission/session issues.

**Fixes Applied:**
1. **Updated OCI VM proxy** with three critical changes:
   - **Force non-streaming:** For all requests, the proxy now rewrites the JSON body to set `"stream": false` before forwarding to Modal. This ensures compatibility with OpenCode's expected response format.
   - **Model ID mapping:** When `model` field equals `"glm-5.1-fp8"`, it is replaced with `"zai-org/GLM-5.1-FP8"` so Modal recognizes the model.
   - **Robustness:** Does not crash if `usage` is `null` in streaming chunks (by avoiding `.pop()` on `None`).

2. **Deployed fixed proxy:**
   - Wrote updated `modal_proxy.py` to `/opt/modal-token-pool/modal_proxy.py` on OCI VM via `scp` and `sudo mv`.
   - Restarted `modal-proxy.service` (systemd) successfully.

3. **OpenCode config:**
   - Added `"permission": "allow"` globally to stop all manual permission prompts.
   - Ensured Modal provider uses proxy baseURL: `http://92.5.60.87:4100/modal/v1` and API key `sk-sin-fleet-master`.
   - Model entry: `modal/glm-5.1-fp8`.
   - Removed experimental `useExtendedResponse` config that was not needed.

**Verification:**
- `curl -X POST http://92.5.60.87:4100/modal/v1/chat/completions -d '{"model":"glm-5.1-fp8",...}'` now returns normal JSON with `"content":"test"`.
- **OpenCode integration still pending final verification** due to possible daemon caching. User should restart OpenCode completely (close terminal/TUI and start fresh) and test:
  ```
  opencode run "echo test" --model modal/glm-5.1-fp8 --format json
  ```
  Expected: The `"part"` with `type: "text"` containing `test`.

---

## 4. Global Auto-Accept Permissions

**User request:** "nie wieder manuell bestätigen allow müssen"

**Fix:** Set global permission to allow all:
```json
"permission": "allow"
```
Applied in both:
- `~/.config/opencode/opencode.json`
- `/Users/jeremy/dev/upgraded-opencode-stack/opencode.json`

Also reinforced agent options: `autoAccept: true`, `confirm: false`.

**Result:** All tools run without manual approval prompts.

---

## Important Notes for Future Maintenance

- **Modal model naming:**  
  Inside OpenCode config always use `modal/glm-5.1-fp8`. The proxy translates it to `zai-org/GLM-5.1-FP8`. Never change the provider's `baseURL` to point directly to Modal; always go through the pool proxy on `92.5.60.87:4100`. ~~(Update: As of the second 2026-04-11 fix, the proxy rate-limits due to an empty key pool. The configuration has been reverted to use the direct API.)~~ **RESOLVED (Part 3 below):** The "empty pool" error was caused by `opencode.json` sending the direct Modal research key to the gateway, which rejected it as `"invalid gateway key"`. The pool always had 3 keys. Config now correctly points at `http://92.5.60.87:4100/modal/v1` with gateway key `sk-sin-fleet-master`.

- **Proxy streaming vs non-streaming:**  
  The proxy now **forces** `stream=false` for every request to maintain OpenCode compatibility. Do not remove this behavior; otherwise, OpenCode will hang or return empty results.

- **Qwen OAuth:**  
  The first login requires the browser step. After the user approves the device code once, the plugin stores the refresh token and rotates automatically. No further manual browser steps needed unless tokens expire.

- **Skills path:**  
  The `skills` key must be an object with `paths` array. Use the schema from this document.

---

## Part 2: Final Model Routing Fixes (2026-04-11)

### 1. `multimodal-looker` Model Reversion
- **Issue:** The `multimodal-looker` agent was accidentally set to use `google/antigravity-gemini-3.1-pro` instead of `google/antigravity-gemini-3-flash`.
- **Fix:** Changed `multimodal-looker` to explicitly use `google/antigravity-gemini-3-flash` in `opencode.json`.
- **Infrastructure Fix:** Updated `install.sh` to enforce the canonical `model`, `description`, and `fallback` configurations from the repository, ensuring these values are merged and not skipped if an agent already exists in a local config.

### 2. `modal/glm-5.1-fp8` Missing ID Bug
- **Issue:** The Model Proxy (`http://92.5.60.87:4100/modal/v1`) failed because it had no active API keys in its pool (`"No Modal API keys available in pool"`).
- **Attempted Fix:** Reverted `baseURL` to the direct Modal API (`https://api.us-west-2.modal.direct/v1`) with the specific `modalresearch_` key.
- **Hidden Bug:** When running `install.sh`, the local `~/.config/opencode/opencode.json` retained its broken configuration because `install.sh` Python merge logic failed to transfer the `"id"` field (`"id": "zai-org/GLM-5.1-FP8"`) if the model node already existed. The `@ai-sdk/openai-compatible` plugin explicitly requires the `id` field for correct routing, otherwise it fails silently or tries to route through the incorrect path.
- **Fix Applied:** Modified `install.sh` to enforce synchronization of the `id` field during model updates.
- **Verification:** Re-ran `./install.sh`. Confirmed `~/.config/opencode/opencode.json` now properly contains `"id": "zai-org/GLM-5.1-FP8"`. The command `opencode run "say hello modal" --model modal/glm-5.1-fp8` executes flawlessly. All configurations synced to the fleet using `sin-sync`.

---

## Outstanding Items

- [x] Correctly point `multimodal-looker` to `google/antigravity-gemini-3-flash` in `opencode.json` and ensure `install.sh` patches agents cleanly.
- [x] Fix `modal/glm-5.1-fp8` model API by pointing back to `https://api.us-west-2.modal.direct/v1` and ensure `install.sh` propagates `"id"` field correctly to all local configs.
- [ ] Complete Qwen OAuth flow (`opencode providers login ...`) and test Qwen model output.
- [ ] Document this RCA in a permanent location (e.g., `~/dev/glm-rotator/ARCHITECTURE.md`).

---

**All changes have been persisted and synced to OCI VM via `sin-sync`.**
**Next session: Test Qwen responses.**
### 3. OCI Pool Proxy Authentication Mismatch
- **Issue:** The Modal proxy on OCI (`http://92.5.60.87:4100/modal/v1`) rejected requests with `{"error": "invalid gateway key"}`. This happened because `opencode.json` was configured to use the direct Modal research key (`modalresearch_...`) instead of the required gateway key (`sk-sin-fleet-master`).
- **Fix:** Pointed the modal provider's `baseURL` to the OCI proxy and updated the `apiKey` to `sk-sin-fleet-master`.
- **Verification:** Successfully executed E2E completion via OCI pool proxy. Propagated the fixed config to all machines via `sin-sync`.

---

## 5. Antigravity Rotation Sync Fixes (2026-04-12)

**Symptom:**
- Google accounts were restored successfully, but OpenCode sessions kept using stale auth data.
- The rotator's attach helper failed against the debug Chrome session with `403 Forbidden` on the DevTools WebSocket.

**Root Cause:**
- `skills/new-google-login/login.py` used `websocket-client` with the default Origin header.
- Chromium rejected the CDP WebSocket handshake from attach mode.
- Running sessions also needed a forced auth/account reload path when `auth.json` or `antigravity-accounts.json` changed.

**Fixes:**
1. CDP attach mode now suppresses the Origin header (`suppress_origin=True`).
2. The login helper is documented as a split flow: standalone AppleScript vs attach-CDP.
3. Antigravity plugin hot-reload patch was added in the stack vendor copy to re-read auth/account mtimes.

**Verification:**
- `python3 -m py_compile` passed for all three login helper copies.
- Rotator logs now show the attach path reaching the CDP stage instead of failing immediately on WebSocket handshake.
