Core commands for this repo on macOS: `git clone https://github.com/Delqhi/upgraded-opencode-stack.git`; `cd upgraded-opencode-stack`; `./install.sh`; `./install.sh --dry-run`; `./install.sh --skip-npm`; `npm run install`; `npm run install:dry-run`; `npm run verify`; `opencode --version`; `ls ~/.config/opencode/skills/ | wc -l`; `sin-sync` (after opencode.json changes per governance); `diff ~/.config/opencode/AGENTS.md AGENTS.md` (suggested by installer when AGENTS already exists). System utilities expected on Darwin: git, ls, diff, node, npm, python3, rsync, chmod, cp, mkdir.