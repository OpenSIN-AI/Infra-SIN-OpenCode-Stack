Core commands: `./install.sh`, `./install.sh --dry-run`, `./install.sh --skip-npm`, `npm run install`, `npm run install:dry-run`, `npm run verify`, `opencode --version`, `ls ~/.config/opencode/skills/ | wc -l`, `sin-sync` after opencode.json changes, `diff ~/.config/opencode/AGENTS.md AGENTS.md`. System utilities expected on Darwin: git, ls, diff, node, npm, python3, rsync, chmod, cp, mkdir.