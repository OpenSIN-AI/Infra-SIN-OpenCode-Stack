Conventions inferred from repo docs and installer: preserve existing user setup; additive-only installs; never overwrite existing configs when avoidable; backup existing ~/.config/opencode/opencode.json before merge; merge only new plugins/providers/models/options/agents/commands; preserve the user's model choice and schema. Documentation style mixes English and German. Repo governance is driven heavily by AGENTS.md and evidence-first verification rules. Shell scripts use bash with set -euo pipefail and explicit logging helpers.