When a task is completed, verify relevant installer/verification commands (`./install.sh --dry-run` when appropriate, `npm run verify`, `opencode --version`, spot-check installed artifacts such as `ls ~/.config/opencode/skills/ | wc -l`). If opencode.json is changed, run `sin-sync` afterward. Preserve additive/no-overwrite behavior and ensure any merge keeps the existing user config/model choice intact. Follow AGENTS.md evidence-first rules and GitHub issue-first process for bugs/incidents.