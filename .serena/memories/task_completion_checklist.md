When a task is completed, verify based on repo docs and governance: run the relevant installer/verification commands (`./install.sh --dry-run` for installer logic checks when appropriate, `npm run verify`, `opencode --version`, and spot-check installed artifacts such as `ls ~/.config/opencode/skills/ | wc -l`). If opencode.json is changed, governance requires `sin-sync` afterward. Preserve additive/no-overwrite behavior and ensure any config merge still keeps existing user config/model choice intact. Follow AGENTS.md evidence-first rules and GitHub issue-first process for bugs/incidents.